{
  "scripts": {
    "docs:dev": "npm run docs:prebuild && vitepress dev",
    "docs:prebuild": "node generate-toc.js && node generate-sidebar.js",
    "docs:build": "npm run docs:prebuild && vitepress build",
    "docs:preview": "vitepress preview"
  },
  "devDependencies": {
    "@types/node": "^24.0.8",
    "vitepress": "^1.6.3"
  },
  "type": "module"
}
